// tenary-operator a shorcut to if{} and else{} kondisi ? nilai_jika_benar : nilai_jika_salah;

// let dark = false;
// let result = dark ? "background dark" : "background light";
// console.log(result);

let age = 21;
let enter =
  age > 20 ? "your age is enough to enter" : "your age is not enough yet";
console.log(enter);
